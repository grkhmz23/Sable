[toolchain]
anchor_version = "0.32.1"
solana_version = "2.2.0"

[features]
seeds = false
skip-lint = false

[programs.localnet]
sable = "SABLE_PROGRAM_ID_TBD"

[programs.devnet]
sable = "SABLE_PROGRAM_ID_TBD"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "localnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "pnpm ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
